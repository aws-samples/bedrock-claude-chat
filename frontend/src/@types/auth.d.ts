// Due to peer dependency issues arising from the introduction of xstate V5, I implemented it by directly defining types instead of through the library.
export type SocialProvider = 'amazon' | 'apple' | 'facebook' | 'google';
